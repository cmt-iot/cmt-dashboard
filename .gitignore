# local secrets
.env
backend/.env
.key

# dependencies / build output
node_modules/
car-monitor/dist/
backend/*.exe

# Node-RED runtime data (bind-mounted into the container). Keep the flow,
# ignore credentials/secrets and generated files.
node-red/data/*
!node-red/data/flows.json
node-red/data/flows_cred.json
